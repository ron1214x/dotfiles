"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const unibeautify_1 = require("unibeautify");
const src_1 = require("../../src");
test(`should successfully beautify JavaScript text `, () => {
    const unibeautify = unibeautify_1.newUnibeautify();
    unibeautify.loadBeautifier(src_1.default);
    const lineBreaks = "\n".repeat(10);
    const preserveLines = "\n".repeat(4);
    const text = `{"test1":1,${lineBreaks}"test2":2}`;
    const beautifierResult = `{\n\t"test1": 1,${preserveLines}\t"test2": 2\n}`;
    return unibeautify
        .beautify({
        languageName: "JSON",
        options: {
            JSON: {
                indent_style: "tab",
                indent_size: 1,
                max_preserve_newlines: 3,
            },
        },
        text,
    })
        .then(results => {
        expect(results).toBe(beautifierResult);
    });
});
//# sourceMappingURL=preserveNewLines.spec.js.map